<%= render "shared/settings_form", model: contact_type_group, title: title, show_active: true, description: "Case contact groups are used to group case contact types. For example a Family group could contain the case contact types: parent, grandpa, aunt." %>
