#!/usr/bin/env bash

bundle exec rubocop --autocorrect --ignore-disable-comments --extra-details
bundle exec erb_lint --lint-all --autocorrect
yarn lint:fix
echo "Linting Factories"
rails factory_bot:lint
