#!/usr/bin/env bash

# bundle exec rubocop --autocorrect --format pacman
bundle exec rubocop --autocorrect --format worst
bundle exec erblint --lint-all --autocorrect
yarn lint:fix
echo "Linting Factories"
rails factory_bot:lint
